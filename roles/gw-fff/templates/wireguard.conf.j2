[Interface]
PrivateKey = {{ item.value.secret_key }}
ListenPort = {{ item.value.listen_port }}

{% for peer in item.value.peers %}

[Peer]
PublicKey = {{ peer.public_key }}
AllowedIPs = {{ peer.allowed_ips }}
{% if peer.endpoint is defined and peer.endpoint|length %}
Endpoint = {{ peer.endpoint }}
{% endif %}
{% if peer.persistent_keepalive  is defined and peer.persistent_keepalive|length %}
PersistentKeepalive = {{ peer.persistent_keepalive }}
{% endif %}

{% endfor %}
